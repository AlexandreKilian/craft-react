{% import "_includes/forms" as forms %}

{{ forms.selectField({
    first: true,
    label: "Rendering",
    name: 'env',
    options: [{value:'server_side', label:'Server Side'},{value:'client_side', label:'Client Side'},{value:'both', label:'Both'}],
    value: settings.env
}) }}

{{ forms.textField({
    label: "Server Bundle Location",
    name: 'serverBundle',
    value: settings.serverBundle
}) }}